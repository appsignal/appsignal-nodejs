{
  "name": "@appsignal/apollo-server",
  "version": "1.0.7",
  "main": "dist/index",
  "types": "dist/index",
  "license": "MIT",
  "dependencies": {
    "@appsignal/types": "~> 2.0",
    "apollo-server-plugin-base": "~> 0.10",
    "tslib": "~> 2.0"
  },
  "devDependencies": {
    "@types/node": "~> 14.14",
    "graphql": "~> 15.4",
    "typescript": "~> 4.1"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json -w --preserveWatchOutput",
    "clean": "rimraf dist coverage",
    "link:npm": "npm link",
    "link:yarn": "yarn link",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch"
  },
  "publishConfig": {
    "access": "public"
  }
}
